<div class="card mb-3 bg-dark text-light">
  <div class="card-header h3">
    <%= icon('fas','igloo', t('.title'), class: 'fa-fw h2 mb-0') %>
    <%= link_to(t('.new'), new_room_path, class: "btn btn-success float-right") %>
  </div>
  <div class="card-body">
    <div class="row">
      <%= render(@rooms) %>
    </div>
  </div>
</div>
