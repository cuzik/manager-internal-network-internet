<div class="card mb-3 bg-dark text-light">
  <div class="card-header h3">
    <%= icon('fas','igloo', t('.title', room_name: @room.name), class: 'fa-fw h2 mb-0') %>
  </div>
  <div class="card-body">
    <%= render 'form', room: @room %>
  </div>
</div>
