<div class="col-xl-3 col-sm-6 mb-3">
  <div class="card text-dark bg-white o-hidden h-100">
    <div class="card-header">
      <%= icon('fas', 'network-wired', t('.card.title'), class: 'fa-fw h5 mb-0') %>
    </div>
    <div class="card-body">
      <div class="card-body-icon">
        <%= icon('fas', 'network-wired', class: 'fa-fw') %>
      </div>
      <div class="mr-5"><%= t('.card.name', switch_name: switch.name) %></div>
    </div>
    <%=link_to(switch, class: 'card-footer text-dark clearfix small z-1') do%>
      <span class="float-left"><%= t('.card.management') %></span>
      <span class="float-right">
        <%= icon('fas', 'angle-right') %>
      </span>
    <% end %>
  </div>
</div>
