<div class="card mb-3 bg-dark text-light">
  <div class="card-header h3">
    <%= icon('fas','user-astronaut', t('.title'), class: 'fa-fw h2 mb-0') %>
    <%= link_to(t('.new'), new_account_user_path, class: "btn btn-primary float-right") %>
  </div>
  <div class="card-body">
    <div class="row">
      <%= render(@users) %>
    </div>
  </div>
</div>
