<div class="card mb-3 bg-dark text-light">
  <div class="card-header h3">
    <%= icon('fas','user-astronaut', t('.title', user_name: @user.name), class: 'fa-fw h2 mb-0') %>
  </div>
  <div class="card-body">
    <%= render 'form', user: @user %>
  </div>
</div>
