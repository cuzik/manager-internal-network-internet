<%= form_with(model: switch, local: true) do |form| %>

  <div class="form-group">
    <div class="form-row">
      <div class="col-md-6">
        <div class="form-group">
          <%= form.label :name %>
          <%= form.text_field :name, autofocus: true, class: "form-control" %>
        </div>
      </div>
      <div class="col-md-6">
        <div class="form-group">
          <%= form.label :mac_address %>
          <%= form.text_field :mac_address, autofocus: true, class: "form-control" %>
        </div>
      </div>
    </div>
  </div>

  <div class="float-right">
    <%= link_to(t('.back'), :back, class: "btn btn-primary") %>
    <%= form.submit switch.name.present? ? t('.edit.confirm') : t('.new.confirm'), class:"btn btn-success" %>
  </div>
<% end %>
