<div class="card mb-3 bg-dark text-light">
  <div class="card-header h3">
    <%= icon('fas','ethernet', t('.title'), class: 'fa-fw h2 mb-0') %>
    <%= link_to(t('.new'), new_port_path, class: "btn btn-primary float-right") %>
  </div>
  <div class="card-body">
    <div class="row">
      <%= render(@ports, back_to_swith: true) %>
    </div>
  </div>
</div>
